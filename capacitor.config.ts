import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'gymApp_labwork6',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
